const validateEmail = require('./validateEmail');
const validatePassword = require('./validatePassword');
const validateToken = require('./validateToken');
const validadeAge = require('./vailidateAge');
const validadeDate = require('./validateDate');
const validateDateAt = require('./validateDateAt');
const validateName = require('./validateName');
const validateRate = require('./validateRate');

module.exports = {
  validateToken,
  validateEmail,
  validatePassword,
  validadeAge,
  validadeDate,
  validateDateAt,
  validateName,
  validateRate,
};
