const errorMiddlewares = require('./error');
const tokennNot = require('./tokennNot');
const tokenInvalido = require('./tokenInvalido');
const tokenName = require('./tokenName');
const tokenAge = require('./tokenAge');
const tokenDate = require('./tokenDate');
const tokenDateObr = require('./tokenDateObr');

module.exports = {
    errorMiddlewares,
    tokennNot,
    tokenInvalido,
    tokenName,
    tokenAge,
    tokenDate,
    tokenDateObr,
};