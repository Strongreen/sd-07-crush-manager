const validateToken = require('./validateToken');
const validateNameCrush = require('./validateNameCrush');
const validateAgeCrush = require('./validateAgeCrush');
const validateDate = require('./validateDate');
const validateRate = require('./validateRate');

module.exports = {
  validateNameCrush,
  validateAgeCrush,
  validateToken,
  validateDate,
  validateRate,
};
