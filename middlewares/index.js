const isValidEmail = require('./isValidEmail');
const isValidPassword = require('./isValidPassword');
const hasToken = require('./hasToken');
const isValidName = require('./isValidName');
const isValidAge = require('./isValidAge');
const isValidDate = require('./isValidDate');

module.exports = { isValidEmail, isValidPassword, hasToken, isValidName, isValidAge, isValidDate };
