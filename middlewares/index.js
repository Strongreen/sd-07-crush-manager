const validateEmail = require('./validateEmail');
const validatePassword = require('./validatePassword');
const validateToken = require('./validateToken');
const validadeAge = require('./validateAge');
const validadeDate = require('./validadeDate');
const validateDateAt = require('./validadeDateAt');
const validateName = require('./validateName');
const validateRate = require('./validateRate');

module.exports = {
  validateToken,
  validateEmail,
  validatePassword,
  validadeAge,
  validadeDate,
  validateDateAt,
  validateName,
  validateRate,
};
