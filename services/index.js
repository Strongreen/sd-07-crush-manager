const getToken = require('./token');
const emailValid = require('./emailValidate');
const passwordValid = require('./passwordValidate');
const read = require('./read');

module.exports = { getToken, emailValid, passwordValid, read };
